# MoonAeroWx 项目申报书

## 一、项目基本信息

- 项目名称：MoonAeroWx
- 项目标识：jshsj124/moonaerowx
- 项目方向：MoonBit 航空气象报文解析与趋势分析工具包
- 项目仓库：https://github.com/jshsj124/MoonAeroWx
- 开源协议：Apache-2.0

## 二、项目简介

MoonAeroWx 是一个使用 MoonBit 实现的航空气象报文解析与趋势分析工具包，面向软件开发、教学演示、离线数据分析和 MoonBit 生态示例建设。项目支持将 METAR/SPECI/TAF 等航空气象文本解析为结构化数据，并提供诊断信息、飞行类别判定、批量统计以及 JSON、Markdown、CSV、文本摘要等多种输出格式。

## 三、项目背景与意义

航空气象报文格式紧凑、缩写多、人工阅读和程序化处理都有一定门槛。MoonAeroWx 通过纯 MoonBit 实现解析器、分类器和输出层，展示 MoonBit 在文本解析、领域建模、命令行工具和多后端可移植场景中的应用价值，也为后续构建 Wasm 演示、数据清洗工具和航空气象教学案例提供基础组件。

## 四、核心功能

1. METAR/SPECI 解析：支持报文类型、站点、观测时间、风、能见度、天气现象、云组、温度/露点、气压、NOSIG 趋势和原始备注保留。
2. RVR 识别：支持跑道视程固定值、可变范围、P/M 限值以及 U/D/N 趋势。
3. TAF 解析：支持 TAF 头部、有效期、初始预报以及 FM、BECMG、TEMPO、PROB、PROB TEMPO 变化组。
4. 趋势时间线：可将 TAF 变化组展开为适合可视化展示的时间线摘要。
5. 结构化诊断：解析错误和警告包含严重级别、诊断码、消息、源码位置和原始 token。
6. 飞行类别判定：支持 VFR、MVFR、IFR、LIFR 分类，并允许配置阈值规则。
7. 多格式输出：支持 JSON、Markdown、CSV 和命令行文本摘要。
8. 批量统计：支持多行 METAR/SPECI 输入，统计解析数量、错误/警告、飞行类别分布、低能见度和低云底事件。

## 五、技术方案

项目采用 token-preserving parser pipeline：先由词法层按空白切分报文并保留源码 span，再由 METAR/SPECI 和 TAF 解析器识别各类气象组，生成强类型结构和诊断信息。分类逻辑与格式化输出解耦，便于独立测试和后续扩展。输出层不依赖外部运行时，尽量保持 native、JS、Wasm、Wasm-GC 等后端的可移植性。

## 六、当前完成情况

项目已完成 MoonBit 模块配置、核心解析器、TAF 时间线、RVR 支持、批量统计、CLI 入口、README、架构文档、功能覆盖矩阵、诊断码文档、局限性说明、示例数据和测试用例。目前本地验证通过 `moon fmt`、`moon check --warn-list +unnecessary_annotation`、`moon test` 和 `moon info`，测试用例 19 个全部通过。

## 七、后续计划

1. 继续完善 METAR/TAF 边界语法和区域扩展支持。
2. 增加跑道状态、风切变、海面状态和更丰富 RMK 备注解析。
3. 完善 TAF 月份跨界语义和复杂嵌套变化组处理。
4. 增加 Wasm 演示页面、更多批量输入适配器和示例数据。
5. 补充 GitHub CI、发布标签和 mooncakes.io 发布准备。

## 八、原创性与开源合规说明

MoonAeroWx 为原创 MoonBit 项目，当前实现未移植或复制第三方解析器代码。项目使用 Apache-2.0 协议开源，示例报文用于功能演示和测试，不作为真实航空运行依据。若后续引入第三方资料、测试数据或参考实现，将在文档中补充来源、许可证和使用范围说明。

## 九、安全边界

MoonAeroWx 仅用于软件开发、教育和离线分析，不是认证航空产品，不能用于真实飞行签派、航前决策、空中运行决策、机场运行最低标准判断或其他安全关键场景。实际航空气象信息应以官方和认证渠道为准。